const debug = require('debug')('dev')

async function postQuotePatrimonialBusiness(context, request, response) {

    debug('iniciando tratativa do endpoint: POST /business/request')
    
    let retorno = {}
    
    debug('finalizando tratativa do endpoint: POST /business/request')

    return retorno
}

module.export = {
    postQuotePatrimonialBusiness
}